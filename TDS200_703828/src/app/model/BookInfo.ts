export class BookInfo {

    constructor(public name: string, public author: string, public category: string,
    public extraInfo, public price: string, public lat: number, public long: number, public id?: string) {}

}